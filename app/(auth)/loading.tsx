/**
 * Auth routes (`/login`, `/signup`, …) stay lightweight — no full-screen loader.
 * Loading overlays belong inside `(app)` via NavigationLoadingGate and segment loaders.
 */
export default function AuthLoading() {
  return null;
}
